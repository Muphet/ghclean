Node.js/Axios script to delete all forked repositories from github (for cleanup).

# Installation
Rename `.env.sample` to `.env` and provide both your github username and [personal API token](https://github.com/settings/tokens) that has at least `repo` and `delete_repo` permissions.